numpy>=1.23
pandas>=1.5
pyyaml>=6.0
pytest>=7.0
# Optional for learned CASA-Net variants and nuPlan integration:
# torch>=2.0
# nuplan-devkit

tqdm>=4.66

# Optional but recommended for exact WGS84 <-> projected CRS transforms in GIS fusion.
pyproj>=3.6
